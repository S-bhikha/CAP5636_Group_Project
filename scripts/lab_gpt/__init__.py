"""Lab GPT (Lane B): decoder-only Transformer, tokenizer, datasets, training loop.

Ported from CAP5636_W6_Transformer(LLM).ipynb into importable modules used by
scripts/train_stage1.py (B0 pretraining) and scripts/train_stage2.py (B1 CPT / M2 SFT).
"""
